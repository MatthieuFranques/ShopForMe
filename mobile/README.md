# mobile

Run the project:

```bash
flutter run
```

Clean the project : 

```bash
flutter clean
```

Refresh deps : 

```bash
flutter pub get
```

### Running Tests

Run the tests with:

```bash
flutter test --coverage
```

---
