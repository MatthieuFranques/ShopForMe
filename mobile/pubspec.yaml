name: mobile
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  http: ^1.3.0
  flutter_bloc: ^8.1.3
  # equatable: ^2.0.5
  flutter_tts: ^4.0.2
  cupertino_icons: ^1.0.8
  logging: ^1.0.2
  equatable: ^2.0.5
  collection: ^1.18.0 
  flutter_blue: ^0.8.0
  permission_handler: ^11.3.1
  cbor: ^6.3.3
  json_annotation: ^4.0.1



  # material_color_utilities: ^0.12.0  # Mettre à jour vers la dernière version
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  test_api: ^0.7.2  # Mettre à jour vers la dernière version
  # leak_tracker: ^10.0.7  # Mettre à jour vers la dernière version
  # leak_tracker_flutter_testing: ^3.0.8  # Mettre à jour vers la dernière version
  string_scanner: ^1.2.0  # Mettre à jour vers la dernière version
  hive_generator: ^2.0.1
  hive_test: ^1.0.1
  mockito: ^5.0.0
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
  assets:
    - assets/datas.json
    - assets/plan.json
    - assets/beacon.json
    - assets/list/defaultShoppingList.json
    - assets/demo/plan28_11_24.json
    - assets/demo/plan_test.json


